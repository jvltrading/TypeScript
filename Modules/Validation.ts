// import { ZipCodeValidator as ZCV } from "./ZipCodeValidator";

// let myZipValidator = new ZCV();

// export interface StringValidator {
//     isAcceptable(s: string): boolean;
// }

// // export * from "./Validation";

export interface StringValidator {
    isAcceptable(s: string): boolean;
}